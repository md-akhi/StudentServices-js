
module.exports = function(ssen) {

  let dirFrelanser = "/frelanser";
  let dirCustomer = "customer";

// middleware function
const Mid = require("../controllers/middleware")(ssen);

  // middleware function to check for logged-in users
  return {
  }
};
